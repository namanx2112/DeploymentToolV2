import { NGForPipePipe } from './ngfor-pipe.pipe';

describe('NGForPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NGForPipePipe();
    expect(pipe).toBeTruthy();
  });
});
